{
  "name": "extension",
  "version": "0.0.1",
  "description": "Project used to bundle JS for Chrome Extension",
  "private": true,
  "scripts": {
    "build": "rm saltyboy.zip && npx webpack --config webpack.config.js && zip -r saltyboy.zip dist/",
    "dev": "npx webpack watch --config webpack.config.js --env dev"
  },
  "keywords": [],
  "author": "Francisco Trindade",
  "license": "MIT",
  "devDependencies": {
    "copy-webpack-plugin": "^10.2.0",
    "terser-webpack-plugin": "^5.3.0",
    "uglify-js": "^3.14.5",
    "webpack": "^5.65.0",
    "webpack-cli": "^4.9.1"
  }
}
